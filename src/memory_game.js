const cards = document.querySelectorAll('.memory-card');

function displayCard(){
this.classiclist.toggle('display');

// for (var i = 0; i < cards.length; i++){
//     cards[i].addEventListener("click", displayCard);
//  };
}